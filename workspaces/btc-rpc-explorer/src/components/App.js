import React from 'react';
import Layout from './Layout';

const App = () => {
  return (
    <Layout>
      {/* Here we'll add the other components as we create them */}
    </Layout>
  );
};

export default App;
