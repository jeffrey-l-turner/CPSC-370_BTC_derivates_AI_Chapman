import React from 'react';

const Layout = ({ children }) => {
  return (
    <div>
      {/* Here we'll add the common layout elements like header, footer, etc. */}
      {children}
    </div>
  );
};

export default Layout;
